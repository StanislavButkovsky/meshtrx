<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/call"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <!-- Табы: Станции / Группы -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabsCallType"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        app:tabMode="fixed"
        app:tabGravity="fill" />

    <!-- Сортировка (для станций) -->
    <LinearLayout
        android:id="@+id/layoutSort"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="4dp">
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="@string/sort_label" android:textSize="12sp" android:textColor="#666" />
        <Button android:id="@+id/btnSortRssi"
            style="?android:attr/buttonBarButtonStyle"
            android:layout_width="wrap_content" android:layout_height="32dp"
            android:text="@string/sort_signal" android:textSize="11sp" />
        <Button android:id="@+id/btnSortName"
            style="?android:attr/buttonBarButtonStyle"
            android:layout_width="wrap_content" android:layout_height="32dp"
            android:text="@string/sort_name" android:textSize="11sp" />
        <Button android:id="@+id/btnSortDist"
            style="?android:attr/buttonBarButtonStyle"
            android:layout_width="wrap_content" android:layout_height="32dp"
            android:text="@string/sort_distance" android:textSize="11sp" />
    </LinearLayout>

    <!-- Список -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Кнопка создать группу (для вкладки Группы) -->
    <Button android:id="@+id/btnNewGroup"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:text="+ Новая группа"
        android:visibility="gone"
        android:layout_marginTop="8dp" />

</LinearLayout>
